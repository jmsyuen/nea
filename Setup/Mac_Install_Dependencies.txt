#run these commands into terminal after installing python

pip install pyinstaller
pip install -U pygame --user

if pip doesn't work run
python3 -m ensurepip
python3 -m pip install pyinstaller
python3 -m pip install -U pygame --user